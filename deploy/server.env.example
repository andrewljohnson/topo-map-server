# Install as /srv/topo-map/server.env on the server (not in Git).
# Use a DNS hostname pointed at this server; Caddy provisions HTTPS.
SITE_ADDRESS=maps.example.com
# Existing, mounted filesystem with approximately 2 TB usable space preferred.
DATA_DIR=/srv/topo-map/data
