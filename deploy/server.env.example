# On the server: /srv/topo-map/server.env (chmod 600)
SITE_ADDRESS=maps.example.com
DATA_DIR=/srv/topo-map/data
SECRETS_DIR=/srv/topo-map/secrets
S3_TILE_BUCKET=your-private-topo-tiles
# Decimal bytes. Warming pauses here; raise deliberately after measuring.
S3_MAX_BYTES=1000000000000
