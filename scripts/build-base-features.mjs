import fs from 'node:fs';
import {createRequire} from 'node:module';
const require=createRequire(new URL('../apps/web/package.json',import.meta.url));const ts=require('typescript');
for(const [name,func]of [['combinedMap','logicalMap'],['terrainRuntime','installDeviceTerrain'],['mapNotes','installMapNotes'],['poiMatching','installPoiMatching'],['amenityImages','installAmenityImages'],['trailBadges','installTrailBadges'],['featureInfo','installFeatureInfo']]){
 const source=fs.readFileSync(new URL('../apps/web/app/'+name+'.ts',import.meta.url),'utf8');
 const code=ts.transpileModule(source,{compilerOptions:{target:ts.ScriptTarget.ES2020,module:ts.ModuleKind.ES2020}}).outputText.replace('export function '+func,'function '+func);
 fs.writeFileSync(new URL('../apps/mobile/src/'+name+'.ts',import.meta.url),'// Generated by scripts/build-base-features.mjs; literal code supports Hermes.\nexport const '+name+'Script = '+JSON.stringify(code)+';\n');
}
